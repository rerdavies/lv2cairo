- Weight style element.
- Table
- rename to "Switch"
- Tab focus for dials.
- Tab focus for switches.
- cab type length off by 4 pixels on Control test page.
- Check windowscale with PNG and transparency.
- File
- Files.



Dependencies:

```
sudo apt install libcairo2-dev
sudo apt install libpangocairo-1.0-0 (LGPL)
sudo apt install cmake
sudo apt install ninja-build (optional)
sudo apt install catch2
sudo apt install librsvg2-dev
sudo apt install lv2-dev
sudo apt install liblilv-dev
```